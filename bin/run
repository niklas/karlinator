#!/usr/bin/env ruby

puts "Karlinator"

module Actions
  def self.run(action)
    puts "#{action}..."
    load_action action
  end

  def self.load_action(action)
    load "actions/#{action}.rb"
  end
end

Actions.run ARGV.shift
